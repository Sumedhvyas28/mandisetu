import 'package:flutter/material.dart';

class DubmyPlaceholder extends StatelessWidget {
  const DubmyPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('placeholder'),
      ),
      body: Center(
        child: Text('placeholder page'),
      ),
    );
  }
}
